apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: egress-pilicy
  namespace: flux-demo
spec:
  podSelector: {}
  policyTypes:
  - Egress
  egress:
  - to:
    - ipBlock:
        cidr: 8.8.8.8/32 # change to cm